const typo = `
  html {
    font-size: 62.5%;
    -webkit-font-smoothing: antialiased;
  }

  /* Heading */
  body,
  button,
  select {
    color: black;
    font-size: 1.6rem;
  }

  /* Normalize */
  strong {
    font-weight: bold;
  }

  em {
    font-style: italic;
  }

  sup,
  sub {
    font-size: 0.6em;
  }

  a {
    color: black;
    text-decoration: none;
  }
`

export default typo
